# Column-level API/schema gate

Conservative table-qualified SQL reference audit completed across active PHP endpoints.

Potential missing/mismatched qualified columns: **2**

Review the findings in COLUMN_COMPATIBILITY_AUDIT.json before release.

This check supplements PHP syntax and table-definition checks; it does not replace runtime integration tests against an actual MySQL/MariaDB database.
